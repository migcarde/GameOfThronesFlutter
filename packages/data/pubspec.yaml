name: data

publish_to: none

version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  #External packages
  http: ^0.13.0
  dartz: ^0.10.0-nullsafety.1
  hive: ^2.0.0
  hive_flutter: ^1.0.0
  path_provider: ^2.0.1
  get_it: ^6.0.0
  equatable: ^2.0.0

  # Personal packages
  domain:
    path: ../domain

  common_flutter:
    path: ../common_flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  # External packages
  mockito: ^5.0.2
  hive_generator: ^1.0.0
  build_runner:
    
flutter:
  uses-material-design: true