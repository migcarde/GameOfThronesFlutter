name: data

publish_to: none

version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  #External packages
  http: ^0.13.0
  dartz: ^0.10.0-nullsafety.1

  # Personal packages
  domain:
    path: ../domain

  common_flutter:
    path: ../common_flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
    
flutter:
  uses-material-design: true